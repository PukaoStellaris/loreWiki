// Shared access gate for the lore pages (/divinity wiki, /story reader).
// Same password and storage key everywhere so unlocking one unlocks both.

// CHANGE THE PASSWORD HERE
export const ACCESS_PASSWORD = "33SECONDS";

export const AUTH_STORAGE_KEY = "lore_wiki_auth";
